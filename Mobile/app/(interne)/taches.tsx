import { RoleScreenPlaceholder } from '@/components/role-screen-placeholder';

export default function TachesScreen() {
  return (
    <RoleScreenPlaceholder
      title="Tâches"
      description="Suivi des tâches assignées, façon kanban (à venir : Milestone 5)."
    />
  );
}
